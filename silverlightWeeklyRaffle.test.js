const silverlightWeeklyRaffle = require('./silverlightWeeklyRaffle')

test('should test the tings', () => {
    // Call the raffle
    // silverlightWeeklyRaffle()

    // Return values or make array for later testing

    expect(true)
  })